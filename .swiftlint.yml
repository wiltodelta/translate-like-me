# SwiftLint configuration for Translate Like Me.
# Run from the repo root: swiftlint

included:
  - Sources

# Build output and the assembled app bundle contain no first-party sources.
excluded:
  - .build

line_length:
  warning: 120
  error: 200
